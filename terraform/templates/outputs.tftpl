This file was generated to help configure CI/CD and manage cluster

## Automated User Credentials

```
AWS_ACCESS_KEY_ID = ${aws_user_id}
AWS_SECRET_ACCESS_KEY = ${aws_user_secret}
```

## Update kubeconfig to access cluster

```
aws eks update-kubeconfig --name ${cluster_name} --region ${aws_region}
```

## ECR Repository URL

```
ECR_REPOSITORY_URL = ${aws_ecr_url}
```